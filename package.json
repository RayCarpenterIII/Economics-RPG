{
  "name": "the-egg-lands",
  "version": "0.2.0",
  "private": true,
  "description": "A browser-first economy RPG with simulated villagers.",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node serve.js --dev",
    "start": "node serve.js",
    "test": "node build.js --check && node tests/simulation-smoke.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
